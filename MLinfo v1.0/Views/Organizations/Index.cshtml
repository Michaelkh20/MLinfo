@model IEnumerable<MLinfo_v1._0.Models.Organization>

@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Organizations List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Organizations" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-square"></i> &nbsp; Create new Organization
            </a>
        </div>
    </div>
    <br />
    <br />

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NameE)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.NameR)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Country)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Authors)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var org in Model)
            {
                <tr>
                    <td>
                        @org.NameE
                    </td>
                    <td>
                        @org.NameR
                    </td>
                    <td>
                        @org.Country.NameE
                    </td>
                    <td>
                        @org.AuthorsToString()
                    </td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller="Organizations" asp-action="Details" asp-route-id="@org.ID"
                           class="btn btn-info mx-2"> <i class="bi bi-x-square"></i> Details</a>
                            <a asp-controller="Organizations" asp-action="Edit" asp-route-id="@org.ID"
                           class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                            <a asp-controller="Organizations" asp-action="Delete" asp-route-id="@org.ID"
                           class="btn btn-danger mx-2"> <i class="bi bi-x-square"></i> Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
